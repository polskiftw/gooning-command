name: Family Recertification Schema Migration Once

on:
  push:
    branches:
      - deduper-cache-foundation
    paths:
      - "tools/migrate_family_recertification_schema_once.py"
      - ".github/workflows/family-recertification-schema-migration-once.yml"

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python tools/migrate_family_recertification_schema_once.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/family-recertification-schema-migration-once.yml tools/migrate_family_recertification_schema_once.py
          git add deduper/family_recertification_queue.py tests/test_family_recertification_schema_migration.py
          git commit -m "Migrate legacy family recertification schema"
          git push origin HEAD:deduper-cache-foundation
