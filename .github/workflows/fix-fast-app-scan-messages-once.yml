name: Fix Fast App Scan Messages Once

on:
  push:
    branches:
      - deduper-cache-foundation
    paths:
      - "tools/fix_fast_app_scan_messages_once.py"
      - ".github/workflows/fix-fast-app-scan-messages-once.yml"

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python tools/fix_fast_app_scan_messages_once.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/fix-fast-app-scan-messages-once.yml tools/fix_fast_app_scan_messages_once.py
          git add deduper/fast_app.py
          git commit -m "Remove obsolete manual scan instructions"
          git push origin HEAD:deduper-cache-foundation
