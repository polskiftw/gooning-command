name: Native UI Refactor Once

on:
  push:
    branches:
      - deduper-cache-foundation
    paths:
      - "tools/native_ui_refactor_once.py"
      - ".github/workflows/native-ui-refactor-once.yml"

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python tools/native_ui_refactor_once.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/native-ui-refactor-once.yml tools/native_ui_refactor_once.py
          git add deduper/app.py deduper/fast_app.py deduper/certified_app.py tests/test_native_ui_architecture.py
          git commit -m "Build review controls natively"
          git push origin HEAD:deduper-cache-foundation
