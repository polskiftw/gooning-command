name: Remove Scan Surface V2 Once

on:
  push:
    branches:
      - deduper-cache-foundation
    paths:
      - "tools/remove_scan_surface_v2.py"
      - ".github/workflows/remove-scan-surface-v2-once.yml"

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python tools/remove_scan_surface_v2.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/remove-scan-surface-v2-once.yml tools/remove_scan_surface_v2.py .github/workflows/remove-remaining-scan-surface-once.yml tools/remove_remaining_scan_surface_once.py
          git add deduper/app.py tests/test_no_manual_scan_surface.py
          git commit -m "Remove remaining manual scan surface"
          git push origin HEAD:deduper-cache-foundation
