# Copy this file to config.txt beside GParty Deduper.exe.
# Fill in the same R2 values used by GParty.

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=

GALLERY_PREFIX=gallery/
INDEX_KEY=gallery-index.json

# NUKE stays locked until this is YES.
ALLOW_DELETE=NO

# Default left-side survivor policy:
# 1 = balanced quality (current behavior)
# 2 = highest resolution
# 3 = largest file size
# 4 = longest duration
# 5 = highest PDQ quality
# Changing this keeps the old queue viewable but deletion-locked until recertified.
SURVIVOR_POLICY=1

# Samples still span the complete GIF/video. 120 is the speed/coverage sweet spot.
VIDEO_SAMPLE_SECONDS=1
MAX_VIDEO_FRAMES=120

# Tuned for Claire's i7-14700KF and fast fiber connection.
SCAN_WORKERS=10
VIDEO_WORKERS=3

# CPU processes used only for pHash, PDQ, and vPDQ comparison. This uses all 20
# physical cores on the i7-14700KF while its remaining hardware threads leave
# scheduling room for previews and Windows.
COMPARE_WORKERS=20
